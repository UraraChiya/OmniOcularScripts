# OmniOcular Scripts
Scripts for Minecraft OmniOcular

# Usage
Place the XML file in the `scripts` directory into the `minecraft/config/OmniOcular directory`, and load the language file under `lang` in any way.
